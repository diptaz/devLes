# 🏗️ Architecture Overview - LesCatur

Visual overview dari arsitektur aplikasi LesCatur dan deployment pipeline.

---

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER DEVICES                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Desktop    │  │    Tablet    │  │    Mobile    │          │
│  │  1920x1080   │  │   768x1024   │  │   375x667    │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                   │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │ HTTPS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      VERCEL CDN (Global)                         │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Edge Network - 40+ Locations Worldwide                    │  │
│  │  • Automatic HTTPS                                         │  │
│  │  • DDoS Protection                                         │  │
│  │  • Asset Caching (1 year)                                  │  │
│  │  • Brotli Compression                                      │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    REACT SPA (Frontend)                          │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  React 18 + TypeScript + Vite                             │  │
│  │                                                            │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │    Pages     │  │  Components  │  │    Utils     │   │  │
│  │  ├──────────────┤  ├──────────────┤  ├──────────────┤   │  │
│  │  │ • Home       │  │ • CourseCard │  │ • API        │   │  │
│  │  │ • Courses    │  │ • Cart       │  │ • Supabase   │   │  │
│  │  │ • Virtual    │  │ • ChessBoard │  │ • Auth       │   │  │
│  │  │ • AI Trainer │  │ • AuthDialog │  │ • Storage    │   │  │
│  │  │ • My Library │  │ • Booking    │  │              │   │  │
│  │  │ • About Us   │  │ • 40+ UI     │  │              │   │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │  │
│  │                                                            │  │
│  │  UI Layer: Tailwind CSS + Radix UI + shadcn/ui           │  │
│  │  State: React Hooks + Context                             │  │
│  │  Routing: Client-side SPA                                 │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ REST API / Realtime
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUPABASE (Backend)                            │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Backend-as-a-Service                                      │  │
│  │                                                            │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │  │
│  │  │   Auth   │  │ Database │  │ Storage  │  │ Realtime │ │  │
│  │  ├──────────┤  ├──────────┤  ├──────────┤  ├──────────┤ │  │
│  │  │ • Email  │  │ • Users  │  │ • Videos │  │ • Live   │ │  │
│  │  │ • OAuth  │  │ • Courses│  │ • Images │  │ • Sync   │ │  │
│  │  │ • JWT    │  │ • Orders │  │ • PDFs   │  │ • Events │ │  │
│  │  │ • RLS    │  │ • Cart   │  │ • Assets │  │          │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │  │
│  │                                                            │  │
│  │  Security: Row Level Security (RLS)                       │  │
│  │  API: Auto-generated REST & GraphQL                       │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### User Journey: Purchase Course

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Browse Courses
     ▼
┌─────────────────┐
│  CourseCard     │ ──┐
│  Component      │   │ Fetch courses
└────┬────────────┘   │ from Supabase
     │                │
     │ 2. Add to Cart ▼
     ▼           ┌──────────┐
┌─────────────┐  │ Supabase │
│    Cart     │◄─┤ Database │
│  Component  │  └──────────┘
└────┬────────┘
     │
     │ 3. Checkout
     ▼
┌─────────────────┐
│   Checkout      │
│   Process       │
└────┬────────────┘
     │
     │ 4. Payment
     ▼
┌─────────────────┐
│   Payment API   │
│  (Mock/Real)    │
└────┬────────────┘
     │
     │ 5. Create Order
     ▼
┌─────────────────┐
│   Supabase      │
│   Orders Table  │
└────┬────────────┘
     │
     │ 6. Grant Access
     ▼
┌─────────────────┐
│   My Library    │ ← User can now access
│   Component     │   purchased content
└─────────────────┘
```

---

## 🏛️ Tech Stack Layers

```
┌────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  UI Framework: React 18                              │  │
│  │  Language: TypeScript                                │  │
│  │  Styling: Tailwind CSS v4                            │  │
│  │  Components: Radix UI + shadcn/ui                    │  │
│  │  Icons: Lucide React                                 │  │
│  │  Notifications: Sonner                               │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│                     BUSINESS LOGIC LAYER                    │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Chess Engine: chess.js                              │  │
│  │  Chess Board: react-chessboard                       │  │
│  │  Forms: react-hook-form + Zod                        │  │
│  │  Date: date-fns                                      │  │
│  │  Charts: Recharts                                    │  │
│  │  State: React Context + Hooks                        │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│                      DATA ACCESS LAYER                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Client: @supabase/supabase-js                       │  │
│  │  API: utils/api.ts                                   │  │
│  │  Auth: utils/supabase/client.ts                      │  │
│  │  Storage: Supabase Storage API                       │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────┐
│                     BACKEND SERVICES                        │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Platform: Supabase                                  │  │
│  │  Database: PostgreSQL                                │  │
│  │  Auth: JWT + RLS                                     │  │
│  │  Storage: S3-compatible                              │  │
│  │  Realtime: WebSockets                                │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Click Login/Register
     ▼
┌──────────────────┐
│  AuthDialog      │
│  Component       │
└────┬─────────────┘
     │
     │ 2. Enter credentials
     ▼
┌──────────────────┐
│  Supabase Auth   │
│  API             │
└────┬─────────────┘
     │
     │ 3. Validate & Generate JWT
     ▼
┌──────────────────┐     ┌─────────────┐
│  JWT Token       │────▶│  Session    │
│  Stored          │     │  Storage    │
└──────────────────┘     └─────────────┘
     │
     │ 4. Token included in all requests
     ▼
┌──────────────────┐
│  Protected       │
│  Routes          │
│  • My Library    │
│  • AI Trainer    │
│  • Bookings      │
└──────────────────┘
```

---

## 🚀 Deployment Pipeline

```
┌───────────────┐
│  Developer    │
│  Local Dev    │
└───────┬───────┘
        │
        │ npm run build
        ▼
┌───────────────┐
│  Build Test   │
│  TypeScript   │
│  ESLint       │
└───────┬───────┘
        │
        │ git push
        ▼
┌───────────────┐
│   GitHub      │
│   Repository  │
└───────┬───────┘
        │
        │ Webhook
        ▼
┌───────────────────────────────────┐
│        Vercel Platform             │
│  ┌─────────────────────────────┐  │
│  │  1. Clone Repository         │  │
│  │  2. Install Dependencies     │  │
│  │  3. Build (vite build)       │  │
│  │  4. Optimize Assets          │  │
│  │  5. Deploy to Edge Network   │  │
│  └─────────────────────────────┘  │
└───────────────┬───────────────────┘
                │
                ├─── Production (main branch)
                │    https://lescatur.vercel.app
                │
                └─── Preview (other branches)
                     https://lescatur-git-[branch].vercel.app
```

---

## 📦 Build Process

```
┌────────────────┐
│  Source Files  │
│  • .tsx        │
│  • .ts         │
│  • .css        │
└───────┬────────┘
        │
        │ TypeScript Compiler
        ▼
┌────────────────┐
│  Type Check    │
│  Transpile     │
└───────┬────────┘
        │
        │ Vite Build
        ▼
┌────────────────────────────┐
│  Bundle & Optimize         │
│  • Code Splitting          │
│  • Tree Shaking           │
│  • Minification           │
│  • Asset Optimization     │
└───────┬────────────────────┘
        │
        ▼
┌────────────────────────────┐
│  dist/ Directory           │
│  ┌──────────────────────┐  │
│  │  index.html          │  │
│  │  assets/             │  │
│  │    ├── index-xx.js   │  │ ← React vendor
│  │    ├── chess-xx.js   │  │ ← Chess vendor
│  │    ├── ui-xx.js      │  │ ← UI vendor
│  │    ├── radix-xx.js   │  │ ← Radix vendor
│  │    └── styles-xx.css │  │
│  └──────────────────────┘  │
└────────────────────────────┘
        │
        │ Deploy to Vercel
        ▼
┌────────────────────────────┐
│  Production URL            │
│  ✓ Optimized               │
│  ✓ Cached                  │
│  ✓ CDN Distributed         │
└────────────────────────────┘
```

---

## 🌐 Network Request Flow

```
User Browser
    │
    │ 1. Request https://lescatur.vercel.app
    ▼
Vercel Edge (Nearest location)
    │
    │ 2. Serve cached HTML
    ▼
User receives index.html
    │
    │ 3. Parse & execute JS bundles
    ▼
React App initializes
    │
    │ 4. Check auth state
    ▼
Supabase Auth Check
    │
    ├─ If authenticated
    │   │
    │   │ 5. Fetch user data
    │   ���
    │  Supabase Database
    │   │
    │   │ 6. Return courses, purchases, etc.
    │   ▼
    │  Render personalized UI
    │
    └─ If not authenticated
        │
        └─ Render public pages
```

---

## 📊 Component Hierarchy

```
App.tsx (Root)
├── AuthDialog
│   ├── Login Form
│   └── Register Form
│
├── Header
│   ├── Logo
│   ├── Navigation
│   ├── Cart Badge
│   └── User Menu
│       ├── My Library
│       ├── My Bookings
│       └── Logout
│
├── Main Content (Tabs)
│   │
│   ├── Courses Tab
│   │   ├── CourseCard (Video) × N
│   │   └── CourseCard (Ebook) × N
│   │
│   ├── Virtual Classes Tab
│   │   ├── TrainerBooking
│   │   │   ├── Trainer List
│   │   │   ├── Calendar
│   │   │   └── Booking Form
│   │   └── VirtualClass (Active Session)
│   │       ├── ChessBoard
│   │       └── Video Conference
│   │
│   ├── AI Trainer Tab
│   │   ├── ChessBoard (Interactive)
│   │   ├── Move History
│   │   ├── AI Analysis
│   │   └── Difficulty Settings
│   │
│   ├── My Library Tab
│   │   ├── Purchased Courses
│   │   │   ├── CourseViewer (Videos)
│   │   │   └── EbookReader (PDFs)
│   │   ├── Subscriptions
│   │   └── Bookings History
│   │
│   └── About Us Tab
│       ├── Team Section
│       ├── Mission
│       └── Contact
│
├── Cart (Sheet)
│   ├── Cart Items
│   ├── Free Trial Items
│   ├── Total Calculation
│   └── Checkout Button
│
├── PricingDialog
│   ├── Subscription Plans
│   └── Payment Options
│
└── Toaster (Notifications)
```

---

## 💾 Database Schema (Simplified)

```
┌──────────────┐
│    users     │ (Supabase Auth)
├──────────────┤
│ id (uuid)    │◄─────┐
│ email        │      │
│ created_at   │      │
└──────────────┘      │
                      │
        ┌─────────────┼─────────────┐
        │             │             │
        │             │             │
┌───────▼──────┐ ┌───▼────────┐ ┌─▼───────────┐
│   courses    │ │  purchases │ │   bookings   │
├──────────────┤ ├────────────┤ ├──────────────┤
│ id           │ │ id         │ │ id           │
│ title        │ │ user_id    │ │ user_id      │
│ price        │ │ course_id  │◄┤ trainer_id   │
│ type         │ │ amount     │ │ date         │
│ content_url  │ │ status     │ │ status       │
└──────────────┘ └────────────┘ └──────────────┘
        │
        │
┌───────▼──────┐
│   cart_items │
├──────────────┤
│ id           │
│ user_id      │
│ course_id    │◄───┘
│ quantity     │
└──────────────┘
```

---

## 🔧 Configuration Files Map

```
Project Root
│
├── package.json ──────────► Dependencies & Scripts
├── vite.config.ts ────────► Build Configuration
├── tsconfig.json ─────────► TypeScript Settings
├── vercel.json ───────────► Deployment Config
├── index.html ────────────► Entry HTML
├── main.tsx ──────────────► React Entry
│
├── .env.example ──────────► Environment Template
├── .gitignore ────────────► Git Exclusions
├── .eslintrc.cjs ─────────► Code Quality Rules
├── postcss.config.js ─────► CSS Processing
│
├── styles/
│   └── globals.css ───────► Global Styles
│
├── components/
│   ├── *.tsx ─────────────► React Components
│   └── ui/ ───────────────► shadcn/ui Components
│
└── utils/
    ├── api.ts ────────────► API Helpers
    └── supabase/ ─────────► Supabase Client
```

---

## 🎯 Performance Optimizations

### Code Splitting Strategy

```
Initial Bundle (Critical)
├── React core
├── Router
└── App shell
    (~150 KB)

Lazy Loaded Chunks
├── CourseViewer ──► Loaded on course open
├── ChessBoard ────► Loaded on chess features
├── AITrainer ─────► Loaded on AI tab
└── EbookReader ───► Loaded on ebook open
```

### Caching Strategy

```
Cache Type         │ Duration  │ Files
───────────────────┼───────────┼─────────────────
Edge Cache         │ Forever   │ Static assets
Browser Cache      │ 1 year    │ Bundled JS/CSS
API Response       │ 5 min     │ Course data
User Session       │ 7 days    │ Auth token
```

---

## 🔒 Security Layers

```
┌─────────────────────────────────────────┐
│  1. HTTPS (Vercel)                      │
│     ✓ TLS 1.3                           │
│     ✓ Certificate Auto-renewal          │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│  2. Authentication (Supabase)           │
│     ✓ JWT Tokens                        │
│     ✓ Secure cookies                    │
│     ✓ Session management                │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│  3. Authorization (RLS)                 │
│     ✓ Row Level Security                │
│     ✓ User-scoped queries               │
│     ✓ Policy enforcement                │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│  4. Input Validation                    │
│     ✓ Zod schemas                       │
│     ✓ Type checking                     │
│     ✓ Sanitization                      │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│  5. Environment Security                │
│     ✓ Env vars not exposed              │
│     ✓ API keys protected                │
│     ✓ No secrets in code                │
└─────────────────────────────────────────┘
```

---

## 📱 Responsive Breakpoints

```
Mobile First Approach:

┌───────────────────────────────────────────┐
│  Mobile: 0px - 640px (sm)                 │
│  • Single column                          │
│  • Hamburger menu                         │
│  • Touch optimized                        │
└───────────────────────────────────────────┘
              ▼
┌───────────────────────────────────────────┐
│  Tablet: 640px - 1024px (md-lg)           │
│  • Two columns                            │
│  • Expanded navigation                    │
│  • Optimized for touch & mouse            │
└───────────────────────────────────────────┘
              ▼
┌───────────────────────────────────────────┐
│  Desktop: 1024px+ (xl-2xl)                │
│  • Multi-column layouts                   │
│  • Full navigation                        │
│  • Mouse optimized                        │
│  • Advanced features visible              │
└───────────────────────────────────────────┘
```

---

## 🎨 Design System Tokens

```
Colors
├── Primary: Blue Pastel (#bfdbfe)
├── Secondary: Indigo (#6366f1)
├── Accent: Purple (#a855f7)
├── Success: Green (#22c55e)
├── Warning: Yellow (#eab308)
└── Error: Red (#ef4444)

Typography
├── Headings: System font stack
├── Body: Inter, sans-serif
└── Code: Monospace

Spacing
├── Base unit: 4px (0.25rem)
├── Scale: 4, 8, 12, 16, 24, 32, 48, 64
└── Container: max-w-7xl

Shadows
├── sm: subtle
├── md: cards
├── lg: modals
└── xl: popovers
```

---

## ✅ Production Checklist

```
┌─ Pre-Launch ─────────────────────────┐
│  [ ] Build successful                 │
│  [ ] All tests pass                   │
│  [ ] No console errors                │
│  [ ] Environment vars set             │
│  [ ] Supabase configured              │
│  [ ] Content loaded                   │
└───────────────────────────────────────┘
             │
┌────────────▼──────────────────────────┐
│  [ ] Deploy to Vercel                 │
│  [ ] Production URL working           │
│  [ ] SSL certificate active           │
└───────────────────────────────────────┘
             │
┌────────────▼──────────────────────────┐
│  [ ] All features functional          │
│  [ ] Mobile responsive                │
│  [ ] Performance > 80                 │
│  [ ] Lighthouse audit pass            │
└───────────────────────────────────────┘
             │
┌────────────▼──────────────────────────┐
│  ✓ Launch Ready!                      │
└───────────────────────────────────────┘
```

---

**Architecture designed for:**
- ⚡ Performance
- 🔒 Security  
- 📱 Responsiveness
- 🚀 Scalability
- 🛠️ Maintainability

**Built with modern best practices for production deployment.**

---

*Last updated: November 2025*  
*Platform: Vercel + Supabase*  
*Framework: React + TypeScript + Vite*
